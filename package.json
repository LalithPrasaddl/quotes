{
  "name": "quotes",
  "private": false,
  "version": "0.0.0",
  "homepage": "https://lalithprasaddl.github.io/quotes",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "vite": "^2.8.0"
  },
  "dependencies": {
    "gh-pages": "^3.2.3"
  }
}
